import React from 'react';

const ScalingBarLoader = () => (
    <div className="flex justify-center items-center h-full">
        <div
            className="w-32 h-2 bg-pink-500 rounded-full transform origin-center"
            id="scaling-bar"
        ></div>
    </div>
);

export default ScalingBarLoader;